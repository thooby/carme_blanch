<%= form_for(@var_conf) do |f| %>
  <% if @var_conf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@var_conf.errors.count, "error") %> prohibited this var_conf from being saved:</h2>

      <ul>
      <% @var_conf.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo %><br />
    <%= f.text_field :titulo %>
  </div>
  <div class="field">
    <%= f.label :name_foto %><br />
    <%= f.text_field :name_foto %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
